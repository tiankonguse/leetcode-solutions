[Q2. 统计区间内的完全 K 次幂数量](https://leetcode.cn/contest/weekly-contest-502/problems/count-k-th-roots-in-a-range/)


给你三个整数 `l` 、 `r` 和 `k` 。

如果存在一个整数 `x` ，使得 `y = x<sup>k</sup>` ，则称整数 `y` 为一个 **完全 k 次幂** 。

返回区间 `[l, r]` （包含两端）内是 **完全 k 次幂** 的整数 `y` 的数量。

**示例 1：**

**输入：** l = 1, r = 9, k = 3

**输出：** 2

**解释：**

区间 `[1, 9]` 内的完全立方数有：

因此，答案为 2。

**示例 2：**

**输入：** l = 8, r = 30, k = 2

**输出：** 3

**解释：**

区间 `[8, 30]` 内的完全平方数有：

因此，答案为 3。

**提示：**

